/**
 * User interface
 */
export interface IUser {
  id: string;
  firstName: string;
  lastName: string;
  role: string;
  enabled: boolean;
  email: string;
  phone: string;
  client: string;
  location: string;
  lastActive: string;
}
